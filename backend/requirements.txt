# Core API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9

# Data + ML (heuristic scorer uses scikit-learn only)
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
pyarrow>=15
joblib>=1.3
deep-translator>=1.11.4

# Optional — drop-in upgrade path to the real XGBoost + TreeSHAP pipeline.
# Uncomment and `pip install -r requirements.txt` to enable app/ml/xgb_scorer.py
# xgboost>=2.0
# shap>=0.44
