# Core API
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9

# Data + ML (heuristic scorer uses scikit-learn only)
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
pyarrow>=15
joblib>=1.3
deep-translator>=1.11.4

# Real Track-1 pipeline: app/ml/xgb_scorer.py loads the artifacts written by the
# repo-root run_pipeline.py (xgboost_model.json, final_calibrator.joblib, ...).
# With these installed and the artifacts present, the backend serves the real
# XGBoost + TreeSHAP model; otherwise it falls back to the scikit-learn scorer.
xgboost>=2.0
shap>=0.44
